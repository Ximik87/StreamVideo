<Window x:Class="Streaming.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local1="clr-namespace:Streaming.WpfApp"
        xmlns:local2="clr-namespace:Streaming.WpfApp.ViewModels"
        mc:Ignorable="d"
        Title="Camera streaming" Height="550" Width="900">
    <Window.DataContext>
        <local2:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local1:BitmapConverter x:Key="BitmapConverter" />
    </Window.Resources>
    <Grid>
        <ItemsControl ItemsSource="{Binding Cameras}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Image 
                            Source="{Binding Image, Converter={StaticResource BitmapConverter}}" 
                            Width="250" 
                            Height="200" 
                            Stretch="Fill" />
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" Rows="2" />                   
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</Window>
